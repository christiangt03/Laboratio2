{
  "name": "lab2-pipeline-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "App mínima para validar el reusable workflow con cache y artifacts",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "build": "esbuild src/index.js --bundle --platform=node --format=esm --outfile=dist/index.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^2.1.0"
  }
}
